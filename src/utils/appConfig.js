// 2.0.1 定义域名根路径
const HTTP_BAST_URL = 'https://mina.itjustfun.cn/api/public/v1/'
// 2.0.2 导出域名根路径,
// export default 只能导出一个对象，因为导出的时候就只有一个，
// 所以 import 的时候，是不需要 { }
export default {
  HTTP_BAST_URL
}
